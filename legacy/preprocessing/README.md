# Pré-processamento legado

Os scripts deste diretório foram movidos intactos de `preprocessing/`. Eles contêm caminhos
absolutos da máquina de origem e não integram o pipeline oficial. Antes de reutilizar qualquer
rotina, migrar seus parâmetros para TOML, substituir caminhos por `ProjectPaths` e adicionar teste.

