PLACEHOLDER — "models"

Checkpoints históricos movidos para:
  artifacts/archive/legacy/gan/checkpoints

Para compatibilidade temporária, crie um symlink local apenas quando necessário:
  ln -s artifacts/archive/legacy/gan/checkpoints models
